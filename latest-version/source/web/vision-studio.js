(function(g){
'use strict';const $=s=>document.querySelector(s);let busy=false,canceled=false,job=null,lastScene=null,lastFile=null,lastResult=null,selected=null,startTime=0;
const repo='https://huggingface.co/Qwen/Qwen3-VL-4B-Instruct-GGUF';
const label={container:'کانتینر',heading:'تیتر',text:'متن',button:'دکمه',image:'تصویر',icon:'آیکن خالی',divider:'جداکننده'};
async function api(path,data){const response=await fetch('/api/ai/'+path,{method:data===undefined?'GET':'POST',headers:{'Content-Type':'application/json','X-H2E-Client':'1'},body:data===undefined?undefined:JSON.stringify(data)});const value=await response.json();if(!response.ok)throw Error(value.error||'خطای سرویس محلی');return value;}
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function message(text,level=''){const n=$('#visionStatus');n.textContent=text;n.className='vision-status '+level;}
function protect(fn){return async()=>{try{await fn()}catch(e){message(e.message,'error');}};}
function buttons(){for(const id of ['visionRun','visionRegister','visionRebuild','visionImage','visionInput','visionModel','visionMode'])$( '#'+id).disabled=busy||(id==='visionRebuild'&&!lastScene);$('#visionCancel').disabled=!busy;}
function files(){const pick=$('#visionImage'),old=pick.value;pick.replaceChildren();(g.H2EApp?.images()||[]).forEach((f,i)=>{const o=document.createElement('option');o.value=String(i);o.textContent=f.name;pick.appendChild(o)});if([...pick.options].some(o=>o.value===old))pick.value=old;if(!busy&&!lastScene)previewOriginal();}
async function previewOriginal(){const f=g.H2EApp?.images()[Number($('#visionImage').value)];if(!f)return;$('#visionSource').src=f.dataUrl;$('#visionBoxes').replaceChildren();}
async function models(){try{const r=await api('models'),pick=$('#visionModel'),old=pick.value;pick.replaceChildren();r.models.filter(m=>m.projector).forEach(m=>{const o=document.createElement('option');o.value=m.id;o.textContent=m.name;pick.appendChild(o)});if([...pick.options].some(o=>o.value===old))pick.value=old;if(!pick.options.length){const o=document.createElement('option');o.value='';o.textContent='ابتدا مدل + mmproj را ثبت کنید';pick.appendChild(o);}}catch(e){message(e.message,'error');}}
function draw(){if(!lastScene)return;$('#visionSource').src=lastFile.dataUrl;const overlay=$('#visionBoxes');overlay.replaceChildren();const all=H2EVisionLayout.flatten(lastScene);const W=lastScene.image.width,H=lastScene.image.height;all.forEach(n=>{const box=document.createElement('button');box.type='button';box.className='vision-box '+(n.type==='container'?'container':'leaf')+(selected===n.id?' selected':'');const [x,y,w,h]=n.box;box.style.cssText=`left:${100*x/W}%;top:${100*y/H}%;width:${100*w/W}%;height:${100*h/H}%`;box.title=label[n.type]+' · '+n.id+(n.text?' · '+n.text:'');box.setAttribute('aria-label',box.title);box.onclick=()=>choose(n.id);overlay.appendChild(box)});const select=$('#visionNode');select.replaceChildren();all.forEach(n=>{const o=document.createElement('option');o.value=n.id;o.textContent=(label[n.type]||n.type)+' · '+(n.text||n.id).slice(0,60);select.appendChild(o)});if(selected)select.value=selected;const counts={};all.forEach(n=>counts[n.type]=(counts[n.type]||0)+1);$('#visionCounts').textContent=Object.entries(counts).map(([t,n])=>label[t]+': '+n).join(' · ');$('#visionWarnings').textContent=(lastResult?.warnings||lastScene.warnings||[]).join('\n');$('#visionReview').hidden=false;$('#visionDownloadPlan').disabled=false;buttons();htmlStage();}
function htmlStage(){
const host=$('#visionHtmlStage');
if(!host||!lastResult||!lastResult.reconstructedHTML)return;
host.hidden=false;
if(!host.dataset.init){
$('#visionHtmlText').value=lastResult.reconstructedHTML;
$('#visionHtmlPreview').srcdoc=lastResult.reconstructedHTML;
H2EChat.mount($('#visionChatHost'),{
title:'مرحلهٔ ۱ — ویرایش HTML با AI',
getHtml:()=>$('#visionHtmlText').value,
getImage:()=>lastFile?lastFile.dataUrl:null,
applyHtml:html=>{
$('#visionHtmlText').value=html;
$('#visionHtmlPreview').srcdoc=html;
lastResult.reconstructedHTML=html;
message('HTML به‌روزرسانی شد؛ حالا «تأیید نهایی و انتقال به مرحلهٔ ۲» را بزنید.','success');
},
afterApply:''
});
$('#visionHtmlText').addEventListener('input',()=>{
$('#visionHtmlPreview').srcdoc=$('#visionHtmlText').value;
lastResult.reconstructedHTML=$('#visionHtmlText').value;
});
$('#visionHandoff').onclick=protect(()=>{
const html=$('#visionHtmlText').value;
if(!html||!html.trim())throw Error('HTML خالی است');
H2EApp.handoffHTML('از-تصویر-'+H2E.slug(lastFile.name)+'.html',html);
message('HTML تاییدشده به مرحلهٔ ۲ رفت؛ حالا آن را به المنتور تبدیل کنید.','success');
});
$('#visionHtmlDownload').onclick=()=>{
if(lastResult.reconstructedHTML)H2EApp.download('reconstructed.html',$('#visionHtmlText').value,'text/html');
};
host.dataset.init='1';
}
else{
$('#visionHtmlText').value=lastResult.reconstructedHTML;
$('#visionHtmlPreview').srcdoc=lastResult.reconstructedHTML;
}
}

function choose(id){selected=id;draw();const n=H2EVisionLayout.flatten(lastScene).find(n=>n.id===id);if(!n)return;$('#visionNodeType').value=n.type;$('#visionNodeType').disabled=!!n.children?.length;$('#visionNodeText').value=n.text||'';$('#visionNodeText').disabled=n.type==='container';$('#visionNodeFont').value=n.style?.font_size||16;$('#visionNodeColor').value=n.style?.color||'#222222';}
async function ensureModel(){await models();const id=$('#visionModel').value;if(!id)throw Error('ابتدا فایل Qwen-VL و mmproj را در بخش آماده‌سازی ثبت کنید. مدل متنی قبلی کافی نیست.');let status=await api('status');const mode=$('#visionMode').value;if(!status.active_model?.vision||status.active_model.id!==id||status.active_model.mode!==mode||status.engine==='stopped'){message('بارگذاری مدل بینایی؛ این مرحله ممکن است چند دقیقه طول بکشد…');await api('models/start',{id,mode});}const deadline=Date.now()+300000;while(Date.now()<deadline){if(canceled)throw Error('تحلیل لغو شد');status=await api('status');if(status.engine==='ready'&&status.active_model?.vision)return;if(status.engine==='stopped')throw Error('موتور بارگذاری نشد؛ لاگ را در کارگاه AI بررسی کنید. فایل mmproj باید با مدل یکسان باشد.');message('در حال بارگذاری مدل… '+Math.round((Date.now()-startTime)/1000)+' ثانیه');await sleep(1200);}throw Error('بارگذاری طولانی شد؛ وضعیت RAM و لاگ موتور را بررسی کنید.');}
async function runImage(file,options={}){
 if(busy)throw Error('تحلیل دیگری در حال اجراست');busy=true;$('#visionReview').hidden=true;canceled=false;job=null;startTime=Date.now();buttons();g.H2EApp.show('vision');
 try{message('آماده‌سازی تصویر…');const prepared=await H2EVisionLayout.prepare(file);$('#visionSource').src=prepared.dataUrl;$('#visionBoxes').replaceChildren();$('#visionProgress').value=2;if(canceled)throw Error('تحلیل لغو شد');await ensureModel();if(canceled)throw Error('تحلیل لغو شد');const response=await api('vision/analyze',{image:prepared.dataUrl,limit:Number($('#visionLimit').value)});job=response.id;const deadline=Date.now()+7200000;
  while(Date.now()<deadline){if(canceled)throw Error('تحلیل لغو شد');const status=await api('vision/jobs/'+job);$('#visionProgress').value=5+Math.round(85*status.done/Math.max(1,status.total));message((status.message||'در حال تحلیل با مدل…')+' · '+Math.round((Date.now()-startTime)/1000)+' ثانیه');if(status.state==='error')throw Error(status.error);if(status.state==='canceled')throw Error('تحلیل لغو شد');if(status.state==='ready'){lastScene=status.result;lastFile=prepared;selected=null;const direction=$('#visionDirection').value;if(direction!=='auto')lastScene.direction=direction;if(prepared.resized)lastScene.warnings.push('تصویر برای مصرف حافظه کمتر به '+prepared.width+'×'+prepared.height+' تغییر اندازه داده شد.');message('ساخت کانتینرها و ویجت‌های بومی…');await sleep(25);lastResult=await H2EVisionLayout.build(lastScene,lastFile,options);draw();choose(H2EVisionLayout.flatten(lastScene).find(n=>n.type!=='container')?.id||lastScene.children[0].id);$('#visionProgress').value=100;message('طرح ساخته شد؛ تمام المان‌ها در ویرایشگر قابل انتخاب و اصلاح‌اند.','success');return lastResult;}await sleep(1100);}
  await api('vision/cancel',{id:job});throw Error('زمان تحلیل بیش از حد شد؛ از تصویر کوتاه‌تر استفاده کنید.');
 }catch(e){message(e.message,'error');throw e;}finally{busy=false;buttons();}
}
async function cancel(){canceled=true;message('در حال لغو تحلیل و توقف مدل…');await api('vision/cancel',{id:job});message('تحلیل لغو و مدل متوقف شد. برای ادامه دوباره اجرا کنید.');}
$('#pane-vision').innerHTML=`<div class="vision-studio"><header class="ai-hero"><div><span class="eyebrow">SCREENSHOT → EDITABLE WIDGETS</span><h2>از تصویر به طرح قابل‌ویرایش</h2><p>Qwen-VL طرح را می‌بیند؛ برنامه از ساختار تشخیص‌داده‌شده کانتینر، تیتر، متن، دکمه، آیکن و تصویر می‌سازد.</p></div><span class="ai-badge">محلی · بدون ارسال تصویر به اینترنت</span></header><details class="vision-setup" open><summary>۱. آماده‌سازی مدل بینایی — فقط بار اول</summary><div class="vision-setup-grid"><div><h3>پیشنهادهای محلی برای VRAM هشت گیگابایتی</h3><b dir="ltr">تحلیل تصویر: Qwen3-VL-8B-Instruct · Q4_K_M + mmproj Q8_0 (حالت GPU+CPU همزمان) · چت‌باکس: Qwen2.5-Coder-7B-Instruct · Q4_K_M (متنی، روی CPU)</b><p>هر دو فایل را از مخزن رسمی یکسان دانلود کنید. اندازهٔ فایل‌ها حدود ۲٫۹۵ گیگابایت است، نه میزان مصرف RAM/VRAM. سازگاری و سرعت روی کارت شما باید بررسی شود؛ RAM سیستم نیز مهم است.</p><div class="vision-links"><a href="${repo}/resolve/main/Qwen3VL-4B-Instruct-Q4_K_M.gguf?download=true" target="_blank" rel="noopener noreferrer">↓ مدل Q4_K_M — حدود ۲٫۵ GB</a><a href="${repo}/resolve/main/mmproj-Qwen3VL-4B-Instruct-Q8_0.gguf?download=true" target="_blank" rel="noopener noreferrer">↓ فایل بینایی mmproj Q8_0 — حدود ۴۵۴ MB</a><a href="${repo}" target="_blank" rel="noopener noreferrer">کارت مدل و مجوز در Hugging Face</a></div><small>دریافت فایل‌ها اینترنت می‌خواهد؛ پس از دریافت، پردازش آفلاین است. مدل‌ها در ZIP برنامه نیستند. حالت «GPU+CPU همزمان» (hybrid) لایه‌های مدل را بین GPU و CPU تقسیم می‌کند و هر دو هم‌زمان کار می‌کنند؛ برای آن موتور engine-cuda لازم است. پس از دانلود، فایل‌ها را در پوشه‌های مدل بگذارید و در «کارگاه AI» از دکمهٔ اسکن دیسک استفاده کنید.</small></div><div><label>مسیر کامل مدل GGUF<input id="visionModelPath" dir="ltr" placeholder="D:\\Models\\Qwen3VL-4B-Instruct-Q4_K_M.gguf"></label><label>مسیر کامل فایل بینایی mmproj<input id="visionProjectorPath" dir="ltr" placeholder="D:\\Models\\mmproj-Qwen3VL-4B-Instruct-Q8_0.gguf"></label><button class="btn btn-primary" id="visionRegister">ثبت این جفت مدل و فایل بینایی</button><p>انتخاب دو فایل از مدل‌های مختلف می‌تواند باعث خطای بارگذاری شود. لازم نیست تصویر را به مدل متنی قبلی بدهید؛ آن مدل تصویر نمی‌خواند.</p><a href="https://github.com/ggml-org/llama.cpp/releases/tag/b11095" target="_blank" rel="noopener noreferrer">موتور CUDA اختیاری و DLLهای متناظر</a></div></div></details><section class="vision-controls"><label class="btn btn-ghost">＋ بارگذاری تصویر طرح<input id="visionInput" type="file" accept="image/png,image/jpeg,image/webp" hidden></label><label>تصویر<select id="visionImage"></select></label><label>مدل بینایی<select id="visionModel"></select></label><label>اجرا<select id="visionMode"><option value="hybrid" selected>GPU+CPU همزمان — پیشنهادی</option><option value="cuda">تمام GPU / CUDA — موتور جداگانه</option><option value="cpu">فقط CPU — آفلاین، کندتر</option></select></label><label>جزئیات در هر بخش<select id="visionLimit"><option value="20">۲۰ المان — سبک</option><option value="30" selected>۳۰ المان — معمولی</option><option value="40">۴۰ المان — بیشتر</option></select></label><label>جهت<select id="visionDirection"><option value="auto">تشخیص مدل</option><option value="rtl">راست‌چین</option><option value="ltr">چپ‌چین</option></select></label></section><div class="ai-row"><button id="visionRun" class="btn btn-primary">۲. تحلیل تصویر و ساخت ویجت‌ها</button><button id="visionCancel" class="btn btn-ghost danger" disabled>لغو و توقف مدل</button><button id="visionRebuild" class="btn btn-ghost" disabled>بازسازی از اصلاحات تشخیص</button><button id="visionDownloadPlan" class="btn btn-ghost" disabled>دانلود ساختار تشخیص</button></div><progress id="visionProgress" max="100" value="0"></progress><p id="visionStatus" class="vision-status" role="status">ابتدا تصویر را وارد کنید. بدون مدل بینایی فعال، خروجی ساختگی یا برش ساده جایگزین تحلیل نمی‌شود.</p><p class="muted">ریسپانسیو از یک تصویر قابل استنتاج قطعی نیست؛ نسخهٔ موبایل پیشنهادی ساخته می‌شود. تصاویر بلند بخش‌بندی می‌شوند؛ برای متن ریز، تصویر هر سکشن جداگانه بهتر است.</p><div id="visionCounts"></div><div class="vision-workspace"><div class="vision-reference"><div class="vision-picture"><img id="visionSource" alt="تصویر مرجع طرح"><div id="visionBoxes"></div></div></div><aside id="visionReview" hidden><h3>۳. بازبینی تشخیص</h3><label>المان تشخیص‌داده‌شده<select id="visionNode"></select></label><label>نوع<select id="visionNodeType">${Object.entries(label).map(([k,v])=>'<option value="'+k+'">'+v+'</option>').join('')}</select></label><label>متن خوانده‌شده<textarea id="visionNodeText" rows="5"></textarea></label><label>اندازه فونت<input id="visionNodeFont" type="number" min="8" max="120" value="16"></label><label>رنگ متن<input id="visionNodeColor" type="color" value="#222222"></label><button id="visionApply" class="btn btn-primary">ثبت اصلاح در ساختار تشخیص</button><small>سپس «بازسازی از اصلاحات» را بزنید. این کار ویرایش دستی خروجی همان تصویر را جایگزین می‌کند. برای اندازه، فاصله، حذف و جابه‌جایی از ویرایشگر خروجی استفاده کنید. فایل تحلیل و HTML بازسازی‌شده مربوط به آخرین بازسازی‌اند؛ ویرایش مستقیم خروجی در JSON و CSS ذخیره می‌شود.</small></aside></div><section id="visionHtmlStage" class="vision-html-stage" hidden><header class="h2e-stage-head"><h3>مرحلهٔ ۱.۵ — خروجی HTML و ویرایش با هوش مصنوعی</h3><p class="muted">HTML بازسازی‌شده از تصویر را ببینید؛ با چت‌باکس، آن را بدون لمس کد اصلاح کنید (مدل متنی روی CPU) یا مستقیم در کادر ویرایش کنید. پس از رضایت، «تأیید نهایی» را بزنید تا HTML به مرحلهٔ ۲ برود.</p></header><div id="visionChatHost" class="h2e-chat-slot"></div><div class="vision-html-cols"><details open><summary>پیش‌نمایش HTML</summary><iframe id="visionHtmlPreview" sandbox="allow-same-origin" title="پیش‌نمایش HTML بازسازی‌شده"></iframe></details><details><summary>کد HTML (ویرایش مستقیم)</summary><textarea id="visionHtmlText" rows="14" dir="ltr" spellcheck="false"></textarea></details></div><div class="ai-row"><button id="visionHandoff" class="btn btn-primary">تأیید نهایی و انتقال به مرحلهٔ ۲ (HTML → المنتور)</button><button id="visionHtmlDownload" class="btn btn-ghost">دانلود HTML</button></div></section><details><summary>موارد نیازمند بازبینی</summary><pre id="visionWarnings" dir="auto"></pre></details></div>`;
$('#visionInput').onchange=protect(async()=>{if($('#visionInput').files.length){lastScene=null;lastResult=null;await H2EApp.addFiles($('#visionInput').files);H2EApp.show('vision');files();}});
$('#visionImage').onchange=()=>{lastScene=null;lastResult=null;selected=null;$('#visionReview').hidden=true;$('#visionCounts').textContent='';$('#visionDownloadPlan').disabled=true;buttons();previewOriginal();};
$('#visionRegister').onclick=protect(async()=>{if(!$('#visionProjectorPath').value.trim())throw Error('مسیر mmproj را هم وارد کنید');const r=await api('models/register',{path:$('#visionModelPath').value,projector:$('#visionProjectorPath').value});await models();$('#visionModel').value=r.id;message('جفت مدل ثبت شد. تصویر را انتخاب کنید و تحلیل را بزنید.','success');});
$('#visionRun').onclick=protect(async()=>{const f=H2EApp.images()[Number($('#visionImage').value)];if(!f)throw Error('ابتدا تصویر طرح را بارگذاری کنید');const r=await runImage(f,H2EApp.options());H2EApp.acceptVision(r);});
$('#visionCancel').onclick=protect(cancel);
$('#visionNode').onchange=e=>choose(e.target.value);
$('#visionApply').onclick=()=>{const n=H2EVisionLayout.flatten(lastScene).find(n=>n.id===selected);if(!n)return;n.type=$('#visionNodeType').value;n.text=$('#visionNodeText').value.slice(0,1200);n.style={...n.style,font_size:Math.max(8,Math.min(120,Number($('#visionNodeFont').value)||16)),color:$('#visionNodeColor').value};draw();message('اصلاح در تشخیص ثبت شد؛ برای اعمال روی خروجی، بازسازی را بزنید.');};
$('#visionRebuild').onclick=protect(async()=>{if(!lastScene)return;if(!confirm('بازسازی، ویرایش‌های دستی خروجی این تصویر را جایگزین می‌کند. ادامه؟'))return;lastResult=await H2EVisionLayout.build(lastScene,lastFile,H2EApp.options());H2EApp.acceptVision(lastResult);htmlStage();message('خروجی از ساختار اصلاح‌شده بازسازی شد.','success');});
$('#visionDownloadPlan').onclick=()=>{if(lastScene)H2EApp.download('vision-layout.json',JSON.stringify(lastScene,null,2),'application/json');};
$('[data-tab="vision"]').addEventListener('click',()=>{files();models();});
document.addEventListener('h2e:files',()=>{files();if(busy&&!H2EApp.images().length)cancel().catch(()=>{});});
g.H2EVision={runImage,cancel,refresh:()=>{files();models();}};
})(window);
